 def findMissing(self, arr, n):
        d=(arr[n-1]-arr[0])//n
        l,u=0.n-1
        while(l<=u):
            m=(l+u)//2
            t=
            if(arr[i+1]-arr[i]==d):
                i+=1
            else:
                return arr[i]+d
#tc: O(n),sc: O(1)